{
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,api-codegen,web-codegen -c blue,magenta,green,yellow 'cd api && bun run bin/dev.ts' 'cd web && bun run watch' 'cd api && bun graphql-codegen --watch' 'cd web && bun graphql-codegen --watch'",
    "dev:test": "concurrently -n api,web 'cd api && ENV_TYPE=test PORT=4445 WEB_URL=http://127.0.0.1:1236 bun run dev' 'cd web && PORT=1236 bun run dev'",
    "deploy": "bun run deploy.js",
    "typecheck": "cd api && bun typecheck && cd ../web && bun typecheck"
  },
  "devDependencies": {
    "@playwright/test": "^1.34.2",
    "@types/node": "^20.2.3",
    "concurrently": "^7.6.0",
    "dotenv-cli": "^6.0.0",
    "prettier": "^2.7.1",
    "prettier-plugin-tailwindcss": "^0.1.13"
  }
}
